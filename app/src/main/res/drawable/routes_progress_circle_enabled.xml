<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/secondaryProgress">
        <shape
            android:innerRadius="6dp"
            android:innerRadiusRatio="8"
            android:shape="ring"
            android:thickness="24dp"
            android:thicknessRatio="20.0"
            android:useLevel="true">


            <gradient
                android:centerColor="@color/colorSuccessLight"
                android:endColor="@color/colorSuccessLight"
                android:startColor="@color/colorSuccessLight"
                android:type="sweep" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">

            <shape
                android:innerRadius="6dp"
                android:innerRadiusRatio="8"
                android:shape="ring"
                android:thickness="24dp"
                android:thicknessRatio="20.0"
                android:useLevel="true">

                <gradient
                    android:centerColor="@color/colorSuccess"
                    android:endColor="@color/colorSuccess"
                    android:startColor="@color/colorSuccess"
                    android:type="sweep" />

            </shape>
        </rotate>
    </item>
</layer-list>
