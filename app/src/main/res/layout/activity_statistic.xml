<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/statistic_appartament_count"
        style="@style/TextViewMainText"
        android:text="@string/apartment_count"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/statistic_appartament_open"
        style="@style/TextViewMainText"
        android:text="@string/apartment_open"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/statistic_appartament_count" />

    <TextView
        android:id="@+id/statistic_signature_count"
        style="@style/TextViewMainText"
        android:text="@string/signs_received"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/statistic_appartament_open" />

</androidx.constraintlayout.widget.ConstraintLayout>